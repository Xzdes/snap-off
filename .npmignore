# Файл: .npmignore
# Назначение: Исключить лишние файлы из публикуемого NPM-пакета.

# 1. Исключаем всё, что не нужно для работы самого пакета

# Папка с компонентами вашего приложения.
# Пользователю пакета не нужны ваши тестовые компоненты.
/src/components/

# Папка с Dev Lab.
# Это инструмент для разработки, а не для конечного пользователя.
/src/lab/

# Тестовые и демонстрационные файлы
# Корневой server.js - это пример использования, а не часть ядра.
/server.js
/public/

# 2. Исключаем конфигурацию разработки и CI/CD
.github/
.circleci/
.travis.yml

# 3. Файлы окружения и секреты (дублируем из .gitignore для надежности)
.env*

# 4. Логи и системный мусор (также дублируем)
*.log
.DS_Store
Thumbs.db

# 5. Конфигурационные файлы инструментов разработки, которые не нужны в пакете.
.vscode/
.idea/
.gitignore
.npmignore
# Другие конфиги, например, для ESLint, Prettier, если они только для разработки.
.eslintrc.js 
.prettierrc